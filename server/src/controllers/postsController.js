// Placeholder posts controller — routes currently implement logic inline in `routes/posts.js`.
// Create controller functions here if you want to refactor route handlers for clarity.

exports.createPost = async (req, res) => {
  res.status(501).json({ error: 'Not implemented — use routes/posts.js for examples' });
};
